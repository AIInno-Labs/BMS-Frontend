/**
 * @deprecated Import from `@/lib/jobData` instead.
 * Kept for backward-compatible imports during the Supabase migration.
 */
export {
  buildScheduleFromJobs,
  completionPercentForJob,
  formatDate,
  formatEstimatedHours,
  formatEstimatedHoursLong,
  formatShortDate,
  formatCreatedDate,
  formatYesNo,
  getReadyToManufactureChartData,
  hasSchedulingBlockAlert,
  isReadyToManufacture,
  jobPriorities,
  jobStatuses,
  resinTypes,
  type ScheduleEntry,
} from "./jobData";
